<Window x:Class="TestVectors.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TestVectors"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    
    <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
        <Border x:Name="bord" BorderBrush="Black" BorderThickness="1" CornerRadius="0" MinWidth="500" Width="Auto" Margin="20,100,20,5">
            <Border.Resources>
                <local:ProportionalHeightConverter x:Key="ProportionalHeightConverter"
                                           MinHeight="300"
                                           MaxHeight="600"/>

                
                
            </Border.Resources>
            <Border.Height>
                <MultiBinding Converter="{StaticResource ProportionalHeightConverter}">
                    <Binding Path="ActualWidth" RelativeSource="{RelativeSource Self}"/>
                    <Binding Path="MinWidth" RelativeSource="{RelativeSource AncestorType=Grid}"/>
                </MultiBinding>
            </Border.Height>
            <Grid x:Name="grd" ShowGridLines="True">
            </Grid>
        </Border>
    </ScrollViewer>
</Window>
